while (Nawal <= Nakhir)
  { Ntengah = (Nawal + Nakhir)/2;
  if (bst > Arr[Ntengah] )
      { Nawal = Ntengah + 1; }
  else if (bst < Arr[Ntengah])
  { Nakhir = Ntengah - 1;}
  else { Nawal = Nakhir +1;
  }
   }
